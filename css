.shake {
  animation: shake 0.5s, flash 0.5s;
}

@keyframes shake {
  0%, 100% { transform: translateX(0); }
  25%, 75% { transform: translateX(-5px); }
  50% { transform: translateX(5px); }
}

@keyframes flash {
  0%, 100% { background-color: inherit; }
  50% { background-color: #ff9999; }
}
